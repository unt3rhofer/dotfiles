xrandr --noprimary
xrandr --output eDP-1 --auto --below HDMI-1-1
xrandr --output HDMI-1-1 --off

  ## to make executable in terminal:
  ## chmod +x /path/to/your/undock.bash
  ## sudo mv /path/to/your/undock.bash /usr/local/bin/undock
